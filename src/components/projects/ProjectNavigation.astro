---
interface ProjectLink {
  slug: string;
  title: string;
}

interface Props {
  prevProject?: ProjectLink;
  nextProject?: ProjectLink;
}

const { prevProject, nextProject } = Astro.props;
---

<nav class="flex flex-wrap items-start sm:items-center justify-between gap-4 py-8 border-t border-gold/20">
  {prevProject ? (
    <a
      href={`/projects/${prevProject.slug}`}
      class="group flex flex-1 min-w-[0] items-center gap-3 text-cream/70 hover:text-gold transition-colors"
    >
      <svg
        class="w-5 h-5 transform group-hover:-translate-x-1 transition-transform"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      <div class="text-left">
        <p class="text-xs uppercase tracking-wider text-taupe">Previous</p>
        <p class="font-serif text-lg leading-tight line-clamp-2">{prevProject.title}</p>
      </div>
    </a>
  ) : (
    <div></div>
  )}

  <a
    href="/projects"
    class="order-3 sm:order-none w-full sm:w-auto text-gold hover:text-gold-light transition-colors text-sm tracking-wider uppercase text-center self-center"
  >
    All Projects
  </a>

  {nextProject ? (
    <a
      href={`/projects/${nextProject.slug}`}
      class="group flex flex-1 min-w-[0] items-center gap-3 text-cream/70 hover:text-gold transition-colors justify-end"
    >
      <div class="text-right">
        <p class="text-xs uppercase tracking-wider text-taupe">Next</p>
        <p class="font-serif text-lg leading-tight line-clamp-2">{nextProject.title}</p>
      </div>
      <svg
        class="w-5 h-5 transform group-hover:translate-x-1 transition-transform"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
      </svg>
    </a>
  ) : (
    <div></div>
  )}
</nav>
